#!/usr/bin/env zsh

function tmux-disk-usage() {
    local percent=$(df -h /dev/sda1 | awk '{ print $5 }' | tail -n 1 | sed 's/%//g')
    if [[ $percent -ge 90 ]]; then
        echo "‼️  #[fg=red,bold]$percent％#[nobold]"
    else
        echo "💾  $percent％"
    fi
}

tmux-disk-usage
