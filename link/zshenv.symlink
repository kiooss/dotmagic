# zsh startup orders {{{
# /etc/zshenv
# ~/.zshenv
# /etc/zprofile
# ~/.zprofile
# /etc/zshrc
# ~/.zshrc
# /etc/zlogin
# ~/.zlogin
# }}}

# export TZ="/usr/share/zoneinfo/Asia/Tokyo"

if [[ -f "$HOME/.cargo/env" ]] then
  source "$HOME/.cargo/env"
fi
