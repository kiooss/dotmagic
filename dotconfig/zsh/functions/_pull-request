#compdef pull-request
#autoload

_pull-request() {
  local stages
  stages=(
    'staging:PR from current branch into the staging branch'
    'production:PR from current branch into the production branch'
    'init:create the staging/production release labels'
  )

  _describe -t stages "Target Stages" stages

  return
}

compdef _pull-request pull-request
